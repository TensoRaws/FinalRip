import type { GlobalThemeOverrides } from 'naive-ui'

export const themeOverrides: GlobalThemeOverrides = {
  common: {
    primaryColor: '#239ff1',
    primaryColorHover: '#0557b4',
    primaryColorPressed: '#1c4cea',
  },
}
